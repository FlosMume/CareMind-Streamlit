# ---- UI ----

# ---- Web UI ----
streamlit==1.49.1

# ---- Vector DB ----
chromadb==0.5.5
pydantic<3
numpy<2.0
pysqlite3-binary==0.5.2.post3

# ---- Embeddings (Torch ≥ 2.6 needed for CVE fix) ----
sentence-transformers==3.0.1
torch==2.6.0
--extra-index-url https://download.pytorch.org/whl/cpu

# 向量与重排（按需）
sentence-transformers>=2.7.0
# cross-encoder 可选：如果你用 CE 重排，就加；否则可注释
# cross-encoder>=0.2.4
streamlit>=1.37
pandas
numpy
chromadb==0.5.*
sentence-transformers
torch
pydantic>=2
scikit-learn
sqlite-utils
pysqlite3-binary==0.5.4 ; python_version >= "3.13"
pysqlite3-binary==0.5.3 ; python_version < "3.13"
chromadb==0.5.23

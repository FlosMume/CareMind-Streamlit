# 基础
streamlit>=1.37
pydantic<2.8
numpy
pandas

# 检索与嵌入（按你的项目选择）
chromadb==0.5.5
sentence-transformers>=2.6.1
# 或者使用 BGE：FlagEmbedding>=1.2.10

# 关键：在 Streamlit Cloud 上提供 SQLite 动态库
pysqlite3-binary==0.5.4

CHROMA_ANONYMIZED_TELEMETRY=false # 有的平台会注入 true，导致“已有实例设置≠当前设置”的冲突
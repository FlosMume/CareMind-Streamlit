--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web UI ---
streamlit==1.49.1

# --- Core ---
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4
typing-extensions>=4.9.0
pydantic>=2.7,<3
httpx==0.27.0
requests>=2.31.0
python-dotenv>=1.0.0

# --- Vector DB / RAG ---
chromadb==0.5.23
sentence-transformers==2.7.0
transformers==4.43.3
scikit-learn==1.4.2

# CPU-only PyTorch (no CUDA on Streamlit Cloud)
torch==2.6.0

# --- SQLite compatibility (only when needed) ---
# For Python 3.10 on Cloud, use this build; remove if stdlib sqlite3 is sufficient.
pysqlite3-binary==0.5.2.post3

# --- Documents / PDF ---
pdfplumber==0.11.4
sqlite-utils>=3.37,<3.39